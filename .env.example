# Backend Environment Variables

# Server configuration
PORT=3001
NODE_ENV=development

# Minio/S3 configuration
MINIO_ENDPOINT=minio
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=tramoya
MINIO_USE_SSL=false

# Logging configuration
LOG_DIR=logs

# Screenshot directory (temporary storage before uploading to Minio)
SCREENSHOT_DIR=screenshots

# Frontend Environment Variables
# These are used during development. In production, the frontend
# is served by Nginx and uses the proxy configuration in nginx.conf.

# API URL for development
VITE_API_URL=http://localhost:3001/api/v1
